import React, { Component } from 'react';

export default class Product extends Component {
  renderList = () => {
  }
  render() {
    this.renderList();
    return (
      <div>
        product list
      </div>
    );
  }
}
